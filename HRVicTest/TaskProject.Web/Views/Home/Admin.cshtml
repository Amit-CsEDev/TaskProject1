@using Microsoft.AspNet.Identity
@{
    ViewBag.Title = "Admin";
}

<h2>Admin</h2>

@if (User.Identity.IsAuthenticated)
{
    <h1>Welcome Admin @(User.Identity.IsAuthenticated ? HttpContext.Current.User.Identity.Name : "Guest")</h1>
    <a href="@Url.Action("SignOut","Home")">Logout</a>
}